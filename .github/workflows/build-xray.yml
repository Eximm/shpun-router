name: Build xray-core (mips_24kc)

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/build-xray.yml"

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      SDK_URL: https://downloads.openwrt.org/releases/24.10.3/targets/ath79/generic/openwrt-sdk-24.10.3-ath79-generic_gcc-13.3.0_musl.Linux-x86_64.tar.zst
      SDK_DIR: openwrt-sdk-24.10.3-ath79-generic_gcc-13.3.0_musl.Linux-x86_64
      TOOLCHAIN_CACHE_KEY: linux-openwrt-24.10.3-ath79-toolchain-v1
      DL_CACHE_KEY: linux-openwrt-24.10.3-ath79-dl-v1

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential libncurses5-dev gawk unzip file \
            libssl-dev wget python3 zstd binutils

      - name: Download & unpack OpenWrt SDK
        run: |
          wget -O sdk.tar.zst "$SDK_URL"
          tar -I zstd -xf sdk.tar.zst
          mv "$SDK_DIR" sdk
          ls -la sdk

      # Кэш скачанных исходников (dl/)
      - name: Restore downloads cache
        id: cache-dl
        uses: actions/cache@v4
        with:
          path: sdk/dl
          key: ${{ env.DL_CACHE_KEY }}

      # Кэш собранного toolchain (staging_dir/)
      - name: Restore toolchain cache
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: sdk/staging_dir
          key: ${{ env.TOOLCHAIN_CACHE_KEY }}

      - name: SDK info
        working-directory: sdk
        run: |
          pwd
          ls -la

      - name: Configure SDK
        working-directory: sdk
        run: |
          make defconfig

      - name: Configure feeds and install xray-core
        working-directory: sdk
        run: |
          ./scripts/feeds update packages
          ./scripts/feeds install xray-core

      - name: Build xray-core package
        working-directory: sdk
        run: |
          make package/xray-core/compile -j"$(nproc)" V=s

      # Сохраняем обновлённые кэши dl/ и staging_dir/
      - name: Save downloads cache (dl)
        if: always()
        uses: actions/cache@v4
        with:
          path: sdk/dl
          key: ${{ env.DL_CACHE_KEY }}

      - name: Save toolchain cache (staging_dir)
        if: always()
        uses: actions/cache@v4
        with:
          path: sdk/staging_dir
          key: ${{ env.TOOLCHAIN_CACHE_KEY }}

      - name: Find built xray-core ipk
        working-directory: sdk
        run: |
          echo "Looking for xray-core ipk..."
          find bin -name "xray-core_*_mips_24kc.ipk" -print

      - name: Extract xray binary from ipk
        working-directory: sdk
        run: |
          set -e

          mkdir -p "$GITHUB_WORKSPACE/output" "$GITHUB_WORKSPACE/ipk-tmp"

          # Находим первый подходящий ipk (абсолютный путь)
          XRAY_IPK=$(find "$PWD/bin" -name "xray-core_*_mips_24kc.ipk" | head -n1)
          echo "Using ipk: $XRAY_IPK"

          if [ -z "$XRAY_IPK" ]; then
            echo "xray-core ipk not found!" >&2
            exit 1
          fi

          echo "IPK stat:"
          ls -lh "$XRAY_IPK" || true
          echo "IPK file type:"
          file "$XRAY_IPK" || true

          cd "$GITHUB_WORKSPACE/ipk-tmp"

          # ipk — это tar: внутри control.tar.* + data.tar.*
          echo "Extract outer tar (ipk)..."
          tar -xf "$XRAY_IPK"

          if ! ls data.tar.* 1>/dev/null 2>&1; then
            echo "No data.tar.* after outer extraction" >&2
            find . -maxdepth 2 -type f | sort
            exit 1
          fi

          DATA_TAR=$(ls data.tar.*)
          echo "Inner DATA_TAR = $DATA_TAR"

          case "$DATA_TAR" in
            *.gz)
              tar -zxf "$DATA_TAR"
              ;;
            *.xz)
              tar -Jxf "$DATA_TAR"
              ;;
            *.zst)
              tar -I zstd -xf "$DATA_TAR"
              ;;
            *)
              echo "Unknown inner data.tar.* compression: $DATA_TAR" >&2
              exit 1
              ;;
          esac

          # Проверяем, что бинарь реально появился
          if [ ! -f usr/bin/xray ]; then
            echo "usr/bin/xray not found after extraction!" >&2
            echo "Listing extracted files (max depth 4):"
            find . -maxdepth 4 -type f | sort
            exit 1
          fi

          # Копируем бинарник xray
          cp usr/bin/xray "$GITHUB_WORKSPACE/output/xray"
          chmod +x "$GITHUB_WORKSPACE/output/xray"

          cd "$GITHUB_WORKSPACE"
          ls -lh output
          file output/xray

      - name: Upload xray binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: xray-mips_24kc
          path: output/xray

#!/bin/sh /etc/rc.common
# shellcheck disable=SC2034

START=95
USE_PROCD=1
SERVICE_DAEMONIZE=0

start_service() {
	procd_open_instance
	procd_set_param command /etc/shpun/agent.sh
	procd_set_param respawn 300 5 0
	procd_close_instance
}

stop_service() {
	# procd сам убьёт процесс
	:
}
